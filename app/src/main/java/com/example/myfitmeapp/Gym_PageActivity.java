package com.example.myfitmeapp;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import androidx.appcompat.app.AppCompatActivity;
import androidx.appcompat.widget.Toolbar;

public class Gym_PageActivity extends AppCompatActivity {

    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_gym__page);

        /*Toolbar mToolbar = (Toolbar) findViewById(R.id.toolbar2);
        mToolbar.setNavigationIcon(R.drawable.back_arrow_black);
        mToolbar.setNavigationOnClickListener(new View.OnClickListener() {

            public final void onClick(View view) {
                Gym_PageActivity.this.lambda$onCreate$0$Gym_PageActivity(view);
            }
        });
        ((Button) findViewById(R.id.button4)).setOnClickListener(new View.OnClickListener() {


            public final void onClick(View view) {
                Gym_PageActivity.this.lambda$onCreate$1$Gym_PageActivity(view);
            }
        });
        ((Button) findViewById(R.id.button5)).setOnClickListener(new View.OnClickListener() {


            public final void onClick(View view) {
                Gym_PageActivity.this.lambda$onCreate$2$Gym_PageActivity(view);
            }
        });
        ((Button) findViewById(R.id.button6)).setOnClickListener(new View.OnClickListener() {


            public final void onClick(View view) {
                Gym_PageActivity.this.lambda$onCreate$3$Gym_PageActivity(view);
            }
        });*/
    }
}
